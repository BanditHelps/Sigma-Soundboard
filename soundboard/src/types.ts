export interface Sound {
    id: string;
    name: string;
    path: string;
    x: number;
    y: number;
}

export interface Theme {
    backgroundColor: string;
    buttonColor: string;
    textColor: string;
}